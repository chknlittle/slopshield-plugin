{
  "name": "slopshield-plugin",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "SlopShield Chrome extension and local mock classification server",
  "scripts": {
    "mock": "node mock-server/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
